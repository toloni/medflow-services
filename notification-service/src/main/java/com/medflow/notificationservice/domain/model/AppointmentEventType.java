package com.medflow.notificationservice.domain.model;

/// The kinds of appointment domain events that can trigger a reminder.
public enum AppointmentEventType {
    APPOINTMENT_CREATED,
    APPOINTMENT_UPDATED
}
